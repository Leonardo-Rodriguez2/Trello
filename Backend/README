

📚 Documentación de Endpoints del Clone de Trello

Este documento detalla todos los endpoints disponibles en la API REST, incluyendo los métodos, rutas y cuerpos de petición (Body JSON) requeridos.

Base URL: http://localhost:3000/api

🔒 Requerimiento de Autenticación (JWT)

Todas las rutas marcadas como (PROTEGIDA) requieren que se envíe el Token JWT obtenido durante el registro o el inicio de sesión.

Header Requerido para Autenticación:

Clave: Authorization

Valor: Bearer <TU_TOKEN_JWT>




👤 Módulo 1: Usuarios (/api/users)

Endpoints

POST /users/register

Propósito: Registra un nuevo usuario en el sistema y devuelve el JWT.

Requerimiento: Abierta (No se necesita token).

POST /users/login

Propósito: Inicia sesión con credenciales y devuelve el Token JWT.

Requerimiento: Abierta (No se necesita token).

JSON Body (POST /users/register)

{
    "username": "nombre_unico",
    "email": "correo@ejemplo.com",
    "password": "miContraseñaSegura123",
    "fullName": "Mi Nombre Completo"
}



JSON Body (POST /users/login)

{
    "email": "correo@ejemplo.com",
    "password": "miContraseñaSegura123"
}



🗃️ Módulo 2: Tableros (Boards - /api/boards)

Endpoints

POST /boards

Propósito: Crea un nuevo tablero y lo asigna al usuario autenticado.

Requerimiento: (PROTEGIDA)

GET /boards

Propósito: Obtiene todos los tableros que pertenecen al usuario.

Requerimiento: (PROTEGIDA)

GET /boards/:id

Propósito: Obtiene un tablero específico por su ID.

Requerimiento: (PROTEGIDA)

GET /boards/:boardId/lists

Propósito: Obtiene todas las listas (columnas) de un tablero.

Requerimiento: (PROTEGIDA)

PUT /boards/:id

Propósito: Actualiza el título, descripción o visibilidad de un tablero.

Requerimiento: (PROTEGIDA)

DELETE /boards/:id

Propósito: Elimina un tablero (y elimina listas/tarjetas por CASCADE).

Requerimiento: (PROTEGIDA)

JSON Body (POST /boards)

Solo title es obligatorio.

{
    "title": "Planificación del Proyecto X",
    "description": "Tablero maestro para el Q3.",
    "is_public": false 
}



JSON Body (PUT /boards/:id)

Solo incluye los campos que deseas modificar.

{
    "title": "Nuevo Título del Proyecto",
    "is_public": true
}



📝 Módulo 3: Listas (Lists - /api/lists)

Endpoints

POST /lists

Propósito: Crea una nueva lista (columna) en un tablero.

Requerimiento: (PROTEGIDA)

PUT /lists/:id

Propósito: Actualiza el título o el orden (order_index) de la lista.

Requerimiento: (PROTEGIDA)

DELETE /lists/:id

Propósito: Elimina una lista (y sus tarjetas asociadas por CASCADE).

Requerimiento: (PROTEGIDA)

JSON Body (POST /lists)

{
    "board_id": 15,
    "title": "Tareas Pendientes" 
}



JSON Body (PUT /lists/:id)

Solo incluye los campos que deseas modificar.

{
    "title": "Nueva Columna - En Espera",
    "order_index": 3
}


🃏 Módulo 4: Tarjetas (Cards - /api/cards)

Endpoints

POST /cards

Propósito: Crea una nueva tarjeta (tarea) en una lista específica.

Requerimiento: (PROTEGIDA)

GET /cards/list/:listId

Propósito: Obtiene todas las tarjetas de una lista, ordenadas por order_index.

Requerimiento: (PROTEGIDA)

GET /cards/:id

Propósito: Obtiene una tarjeta específica por su ID.

Requerimiento: (PROTEGIDA)

PUT /cards/:id

Propósito: Actualiza cualquier campo de la tarjeta (título, descripción, fecha) o la mueve a otra lista/posición.

Requerimiento: (PROTEGIDA)

DELETE /cards/:id

Propósito: Elimina una tarjeta.

Requerimiento: (PROTEGIDA)

JSON Body (POST /cards)

list_id y title son obligatorios.

{
    "list_id": 15,
    "title": "Implementar el módulo de asignaciones",
    "description": "Crear la tabla card_assignments y su controlador.",
    "due_date": "2025-11-15"
}


JSON Body (PUT /cards/:id)

Solo incluye los campos que deseas modificar. Usar list_id y order_index para mover la tarjeta entre listas.

{
    "title": "Revisión Final de Documentación",
    "list_id": 16,
    "order_index": 0
}
